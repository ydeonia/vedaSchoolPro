// Basic smoke test for VedaFlow App
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — full widget tests to be added later
    expect(1 + 1, equals(2));
  });
}
